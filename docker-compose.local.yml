services:
  openclaw:
    image: openclaw:local
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    environment:
      # Provider (at least one required)
      - OPENCODE_API_KEY=${OPENCODE_API_KEY}
      - OPENCLAW_PRIMARY_MODEL=opencode/kimi-k2.5
      - OLLAMA_BASE_URL=http://100.98.78.106:8000/v1
      # Auth
      - AUTH_USERNAME=${SERVICE_USER_OPENCLAW:-admin}
      - AUTH_PASSWORD=${SERVICE_PASSWORD_OPENCLAW:-}
      - AUTH_AUTO_GENERATE=${AUTH_AUTO_GENERATE:-true}
      - OPENCLAW_GATEWAY_TOKEN=${SERVICE_PASSWORD_OPENCLAW_GATEWAY:-}
      - OPENCLAW_GATEWAY_TOKEN_AUTO_GENERATE=${OPENCLAW_GATEWAY_TOKEN_AUTO_GENERATE:-true}
      # Browser sidecar
      - BROWSER_CDP_URL=http://browser:9223
      - BROWSER_DEFAULT_PROFILE=openclaw
      - BROWSER_EVALUATE_ENABLED=true
    volumes:
      - openclaw-data:/data
      - ./my-openclaw.json:/app/config/openclaw.json:ro
    depends_on:
      - browser
    restart: unless-stopped

  browser:
    image: openclaw-browser:local
    build:
      context: .
      dockerfile: Dockerfile.browser
    expose:
      - "3000"
      - "9223"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - CHROME_CLI=--remote-debugging-port=9222
    volumes:
      - browser-data:/config
    shm_size: 2g
    restart: unless-stopped

volumes:
  openclaw-data:
  browser-data:
